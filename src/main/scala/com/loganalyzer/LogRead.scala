package com.loganalyzer

import scala.concurrent.Future

object LogRead {
  var logMessages = Seq.empty
  var status = "File reading isn't completed yet"
//  def readLogFile(location: String): Future[Seq[LogData]] = {
//    val source = scala.io.Source.fromFile(location)
//
//  }
}
