app {
  routes {
    # If ask takes more time than this to complete the request is failed
    ask-timeout = 5s
  },
  http {
    host = "localhost"
    port = 3000
  },
  log-file {
    location = "/media/anwar/20FD1C4D6D67341F/Development/Projects/log-analyzer/log-analyzer/src/main/resources/messages.log"
  },
  sqlite {
    connectionString = "jdbc:sqlite:"
    fileName = "logs.db"
  }
}
